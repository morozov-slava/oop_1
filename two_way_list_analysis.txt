Большая часть АТД перекочевала из предыдущего задания, поэтому заострю внимание на тех аспектах, по которым были расхождения.

Я добавил все необходимые статусы, но также добавил несколько тех, которых нет в эталонном решении.

Добавил `get_add_to_empty_status()`, т.к. предусловием установил "список должен быть пустым". В задании по `LinkedList` данный метод предлагался для реализации, но в эталонном решении его, к сожалению, нет.

Добавил методы `get_is_head_status()` и `get_is_tail_status()` по аналогии с предыдущим решением (справедливо будет сказать, что я забыл их исключить после изучения эталонного решения для предыдущего задания,
однако, мне, к сожалению, не очень понятна причина такого АТД для этих методов).

По дочернему классу `TwoWayList` реализация соответствует эталонной.
